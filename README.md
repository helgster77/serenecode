<p align="center">
  <img src="serenecode.jpg" alt="SereneCode" width="500">
</p>

<h3 align="center">A Framework for AI-Driven Development of Verifiable Systems</h3>

SereneCode is a formal verification framework for AI-generated Python. It tells the AI *how* to write verifiable code, checks that the AI followed instructions, and then *verifies* the code does what it claims — using symbolic execution and SMT solvers, not just tests.

> **This framework was built from scratch by AI, governed by its own rules.** SereneCode's SERENECODE.md was written before the first line of code. Every function, every class, every module in the framework was then generated by AI following those conventions — and verified by the framework itself. 166 functions verified through Level 5. 166 contracts. 411 tests. Zero failures. The tool verifies itself.

---

## Why This Exists

AI writes code fast. But *fast* and *correct* aren't the same thing. When you're building a medical dosage calculator, a financial ledger, or an avionics controller, "it passed my tests" isn't enough. Tests check the inputs you thought of. Formal verification uses an SMT solver to search for *any* input that breaks your contracts.

The problem is that formal verification has always been expensive — too slow, too manual, too specialized. SereneCode makes it tractable by controlling the process from the start: a convention file tells the AI to write verification-ready code, a structural linter checks it followed the rules, and CrossHair + Z3 verify correctness via symbolic execution.

SereneCode is designed for **building new verifiable systems from scratch with AI**, not for retrofitting verification onto large existing codebases. The conventions go in before the first line of code, and every module is written with verification in mind from day one. That's what makes it work.

---

## See It In Action: The Medical Dosage Calculator

We built the same medical dosage calculator twice from the same spec — once with plain AI, once with SereneCode — to show the difference.

Both versions implement four functions: dose calculation with weight-based dosing and max caps, renal function adjustment with tiered CrCl thresholds, daily safety checks with exact Decimal arithmetic, and contraindication detection across current medications.

Both versions are functionally correct. Here's what's different:

| What can you claim? | Plain AI (142 lines) | SereneCode (519 lines) |
|---|---|---|
| **Dose never exceeds maximum** | Tested with 6 inputs | Postcondition checked on every call; CrossHair finds no counterexample |
| **Renal adjustment never increases a dose** | Tested with 10 CrCl values | `result <= dose_mg` contract enforced on every execution |
| **Safety result is internally consistent** | No validation — you can construct `SafetyResult(total=9999, max=100, is_safe=True)` | 5 invariants make inconsistent state structurally impossible |
| **Objects are truly immutable** | `frozen=True` with mutable `set` on Drug | `_Frozen` mixin + `frozenset` — fully locked down |
| **Boundary behavior (CrCl exactly 30.0)** | 4 boundary tests | Contract holds at every boundary; solver explores symbolically |
| **What if someone changes the code later?** | Hope the tests catch it | Contracts are part of the function — every call is checked at runtime |
| **Can a solver verify it?** | Nothing to verify against | 72 contracts: 23 invariants, 27 preconditions, 22 postconditions |
| **Confidence shipping to a hospital?** | Moderate — well tested | High — formally specified, runtime-enforced, solver-verified |

The plain version has 59 tests that check specific inputs. The SereneCode version has contracts that define *what correct means* — and every call to every function is checked against them at runtime. CrossHair and Z3 can then symbolically verify those contracts hold, searching for counterexamples across the entire input space within analysis bounds.

> Both examples live in [`examples/dosage-regular/`](examples/dosage-regular/) and [`examples/dosage-serenecode/`](examples/dosage-serenecode/). Read them side by side.

---

## How It Works

### 1. SERENECODE.md — Your AI Writes Code That's Built for Verification

A markdown file in your project root that tells AI coding agents exactly how to write code: what contracts to include, what architecture to follow, what patterns to use. When Claude Code (or any AI agent) reads this before generating code, the output is verification-ready from the first keystroke.

```bash
serenecode init              # balanced defaults — contracts on public functions, hexagonal architecture
serenecode init --strict     # maximum rigor — contracts on ALL functions (public and private), no exemptions
serenecode init --minimal    # lightweight — contracts on public functions only, relaxed architecture rules
```

This creates a SERENECODE.md tailored to your project and integrates with CLAUDE.md so Claude Code follows the conventions automatically. `--strict` is maximum rigor for mission-critical code. The default is what SereneCode uses on itself. `--minimal` is a good starting point if you're exploring. You write the rules once — the AI follows them forever.

### 2. The Checker — Instant Feedback

A lightweight AST-based linter that validates code follows SERENECODE.md conventions in seconds. Missing a postcondition? No class invariant? I/O imports in a core module? Caught before you waste time on heavy verification.

```bash
serenecode check src/ --structural    # seconds
```

### 3. The Verifier — Symbolic Verification

A five-level verification pipeline that escalates from fast checks to full symbolic verification:

| Level | What | Speed | Backend |
|-------|------|-------|---------|
| **L1** | Structural conventions | Seconds | AST analysis |
| **L2** | Type correctness | Seconds | mypy --strict |
| **L3** | Property-based testing | Seconds–minutes | Hypothesis |
| **L4** | Symbolic verification | Minutes | CrossHair / Z3 |
| **L5** | Cross-module verification | Seconds | Compositional analysis |

```bash
serenecode check src/ --level 4       # verify it
```

The full pipeline is thorough but not instant — it's fully multithreaded, and verifying SereneCode's own 388 functions through L5 took about 14 minutes on a MacBook Pro (almost entirely L4 symbolic verification; L5 adds seconds). Larger systems will take longer. Not everything needs L4/L5. Critical paths get full symbolic and compositional verification. Utility functions get property testing. You decide.

---

## The AI Agent Loop

SereneCode is designed for AI agents that write code and fix their own mistakes:

```
AI reads SERENECODE.md           → knows how to write verification-ready code
AI generates code with contracts → preconditions, postconditions, invariants
serenecode check --structural    → instant: did the AI follow the rules?
serenecode check --level 4       → deep: can the solver find any counterexample?
AI reads counterexamples         → "input x=[-1] violates postcondition"
AI fixes the code                → adjusts implementation or contract
Repeat until verified            → no counterexample found, not just tested
```

AI-generated code won't always pass verification on the first try — and that's the point. SereneCode gives the coding agent structured feedback on exactly what failed and why: counterexamples, violated contracts, and suggested fixes. The agent uses that feedback to iterate until the code passes. The value isn't in one-shotting perfection — it's in the loop that converges on verified correctness.

Works in Claude Code, works in the terminal, works in CI:

```python
import serenecode

result = serenecode.check(path="src/", level=4)
for failure in result.failures:
    print(failure.counterexample)  # exact input that breaks the code
    print(failure.suggestion)      # proposed fix direction
```

---

## Built With Its Own Medicine

SereneCode isn't just a tool that *tells* you to write verified code. It *is* verified code.

The SERENECODE.md convention file was the first artifact created — before any Python was written. Every module in the framework was then built by AI following those conventions. Then we ran the full verification pipeline through Level 5 on the entire codebase — 14 minutes on a MacBook Pro:

```
388 functions checked, 388 passed, 0 failed, 0 skipped
```

Every function in the framework passes its own verification through Level 5. The numbers:

- **166 functions** verified through Level 5 — zero failures, zero skipped
- **166 contracts** across the codebase (20 invariants, 72 preconditions, 75 postconditions) — every contract enforces a real property, no placeholders
- **411 tests** including property-based tests and integration tests
- **Zero** `mypy --strict` errors
- **Hexagonal architecture** enforced — no I/O in core modules, all dependencies injected through Protocols

At Level 4, CrossHair and Z3 search for counterexamples across every function in the codebase via symbolic execution. Level 5 adds structural compositional analysis: dependency direction, circular dependency detection, interface compliance, contract presence at module boundaries, and architectural invariants. Together, they provide both deep per-function verification and system-level structural guarantees.

---

## Quick Start

```bash
# Clone and install from source
git clone https://github.com/helgster77/serenecode.git
cd serenecode
pip install -e .

# Initialize a project with conventions
serenecode init

# Let your AI agent write code following SERENECODE.md...
# Then verify:
serenecode check src/

# Or go deep:
serenecode check src/core/ --level 4 --format json
```

## CLI Reference

```bash
serenecode init [--strict | --minimal]          # set up conventions
serenecode check [<path>] [--level 1-5]         # run verification
serenecode check --structural [<path>]          # L1 only (fast)
serenecode check --verify [<path>]              # L3-5 (deep)
serenecode status [<path>]                      # verification status
serenecode report [<path>] [--format html]      # generate reports
```

**Exit codes:** 0 = passed, 1 = structural, 2 = types, 3 = properties, 4 = symbolic, 5 = compositional, 10 = internal error.

---

## Honest Limitations

SereneCode is honest about what it can and can't do:

**Symbolic verification is not mathematical proof.** CrossHair uses bounded symbolic execution backed by Z3 — it explores execution paths within time limits (default 60 seconds per function) and searches for counterexamples. If it finds none, that's strong evidence of correctness, but it's not an unbounded proof in the Coq/Lean sense. For pure functions with simple control flow, the coverage is effectively exhaustive. For complex code, it's bounded.

**Contracts are only as good as you write them.** A function with weak postconditions will "verify" even if the implementation is subtly wrong. SereneCode checks that contracts exist and hold, but can't check that they fully capture your intent.

**Runtime checks can be disabled.** icontract decorators are checked on every call by default, but can be disabled via environment variables for performance in production. This is a feature, not a bug — but it means runtime guarantees depend on configuration.

**Not everything should be L4.** Symbolic verification is slow (minutes per function). Use it for critical business logic, not CLI glue. Mixed verification levels exist for this reason.

**Level 5 is structural, not semantic.** Compositional verification (L5) checks that contracts *exist* at module boundaries, that dependency direction is correct, and that interfaces structurally match. It does not verify that postconditions *logically satisfy* preconditions across call chains — that would require symbolic reasoning across module boundaries, which is a planned future enhancement. L5 catches architectural violations and contract gaps, not logical insufficiency.

**icontract-hypothesis compatibility.** The `icontract-hypothesis` bridge is currently incompatible with Python 3.14. SereneCode derives Hypothesis strategies from type annotations as a workaround.

---

## Architecture

SereneCode follows hexagonal architecture — the same pattern it enforces on your code:

```
CLI / Library API           ← composition roots
    │
    ├──▸ Pipeline           ← orchestrates L1 → L2 → L3 → L4 → L5
    │       ├──▸ Structural Checker    (ast)
    │       ├──▸ Type Checker          (mypy)
    │       ├──▸ Property Tester       (Hypothesis)
    │       ├──▸ Symbolic Verifier     (CrossHair/Z3)
    │       └──▸ Compositional Checker (ast)
    │
    ├──▸ Reporter           ← human / JSON / HTML
    │
    └──▸ Adapters → Ports   ← Protocol interfaces for all I/O
```

Core logic is pure. All I/O goes through Protocol-defined ports. The verification engine itself is verifiable.

## License

MIT